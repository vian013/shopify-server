const productQueries = require("./products.query")
const cartQueries = require("./cart.query")
const blogQueries = require("./blogs.query")
const collectionQueries = require("./collections.query")

module.exports = {
    productQueries,
    cartQueries,
    blogQueries,
    collectionQueries
}
