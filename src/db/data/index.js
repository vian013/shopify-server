const {productData, productByHandleData} = require("./products.data")
const {cartData} = require("./cart.data")
const { collectionByHandleData } = require("./collections")
const { blogData } = require("./blogs.data")

module.exports = {
    productData,
    productByHandleData,
    cartData,
    collectionByHandleData,
    blogData
}